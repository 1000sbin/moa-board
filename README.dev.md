# 모아보드 (Moaboard)

윈도우 데스크톱 작업 대시보드.
워크 트래커 자동 감지 · 캘린더 · 할 일 · 일정/D-day · 습관 · 메모 · 이번 달 목표 · 연간 목표.
**모든 데이터는 로컬 SQLite에 저장. 서버 없음.**

> 이 버전은 **윈도우 전용**이야.

## 준비물
- Windows 10 / 11 (x64)
- Node.js 20 이상 (LTS 권장)

## 처음 실행

```bash
npm install          # 의존성 설치 (electron만 받으면 됨)
npm start            # 앱 실행
```

> SQLite는 **Node 내장 모듈(node:sqlite)**을 써서 별도 빌드 도구(Visual Studio C++ 등)가 필요 없어.
> `npm install`은 electron만 받으면 되니까 빠르게 끝나.

## 개발 모드 (DevTools 열림)

```bash
npm run dev
```

## 배포 빌드 (윈도우)

```bash
npm run dist            # 설치 파일(NSIS .exe) + 포터블 .exe 둘 다
npm run dist:portable   # 포터블 단일 exe만 (설치 없이 실행)
```

- **NSIS**: 설치형. 바탕화면·시작메뉴 바로가기 생성, 설치 경로 선택 가능.
- **Portable**: 설치 없이 exe 하나로 실행. USB에 넣고 다녀도 됨.
- 결과물은 `dist/` 폴더에 생성돼.

## 깃허브 릴리스 & 자동 업데이트

```bash
npm run release        # 빌드 + 깃허브 릴리스에 자동 업로드
```

- `package.json`의 `version`을 올린 뒤 `npm run release` 실행하면, 빌드된 설치파일이 깃허브 릴리스로 올라가.
- 실행하려면 깃허브 토큰이 필요해: 환경변수 `GH_TOKEN`에 설정 (repo 권한).
- 사용자 앱은 켤 때마다 새 버전을 확인하고, 있으면 자동으로 받아서 다음 실행 때 적용해.

## 상시 실행 & 자동 시작
- 창을 닫아도 **트레이(작업표시줄 우하단)로 숨어서 계속 돌아가.** 워크 트래커가 백그라운드로 시간 기록.
- 완전 종료는 트레이 아이콘 우클릭 → 종료.
- 윈도우 부팅 시 자동 시작 (설정에서 끌 수 있음). 부팅 시엔 창 없이 트레이로만 조용히 시작.

## 구조

```
src/
  main/
    main.js       메인 프로세스 (창·트레이·자동시작·IPC)
    preload.js    렌더러에 노출하는 안전한 API (window.moa.*)
    tracker.js    워크 트래커 자동 감지 엔진 (active-win)
  db/
    db.js         SQLite 스키마 + 초기화
  renderer/
    index.html    UI
    assets/       아이콘 (icon.ico, icon.png, tray.png, icon.svg)
```

## 데이터 위치
- `%APPDATA%/moaboard/moaboard.db`

## 워크 트래커 작동 방식
1. active-win으로 5초마다 최상단 창의 **프로세스명**을 읽음 (예: `CLIPStudioPaint.exe`)
2. `app_rule` 테이블의 매핑 규칙과 대조 → 카테고리 결정 (예: 그림 작업)
3. 카테고리별로 세션 시간 누적. 카테고리 바뀌면 새 세션.
4. 90초 이상 입력 없으면(자리 비움) 세션 자동 정지 → 시간 부풀지 않음.
5. 기본 규칙: CLIP STUDIO/Photoshop→그림, VS Code/Visual Studio→코딩, 한글/Word→글·기획, Chrome/Firefox→리서치. (편집 가능)

## 기능 (전부 로컬 DB 연결 완료)
- [x] 워크 트래커 — 활성 프로그램 자동 감지 + 카테고리별 시간 누적
- [x] 프로그램 규칙 편집 — 감지된 프로그램을 카테고리에 지정 (아이콘 선택 포함)
- [x] 할 일 — 추가/완료/수정/삭제, 진행률 연동
- [x] 일정 / D-day — 추가/수정/삭제, 마감 표시 → D-day 자동 반영
- [x] 캘린더 — 월간 달력에 일정 표시, 날짜 클릭으로 일정 추가
- [x] 습관 — 요일별 체크(일~토), 항목 추가/삭제
- [x] 메모 — 작성/편집/삭제, 태그 필터
- [x] 이번 달 목표 — 수치/작업시간/체크 3종 (시간형은 트래커 자동 집계)
- [x] 연간 목표 — 마일스톤 완료율로 진행률 자동 계산
- [x] 자동 업데이트 — 깃허브 릴리스에서 새 버전 감지

## 다음 할 일 (로드맵)
- [ ] 구글 캘린더 읽기 전용 연동
- [ ] 클라우드 동기화 (여러 기기에서 같은 데이터)
- [ ] 안드로이드 앱 (보기/입력용)
